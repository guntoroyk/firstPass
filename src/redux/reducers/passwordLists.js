const initialState = {
  passwords: 'kosong'
}

const passwordListsReducer = (state = initialState, action) => {
  switch (action.type) {
    
    default: 
      return state
  }
}

export default passwordListsReducer
